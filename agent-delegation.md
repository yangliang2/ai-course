# 如何放心地让 Agent 开展工作

> 一篇关于 Agent 可委托性的认知对齐材料。它不提供安慰，只提供判据。

---

## 一、先立对问题

过去两年，几乎每家公司都在做 Agent。客服、代码审查、数据分析、招投标——Demo 一个比一个漂亮。但走到一线问一句：“有多少任务，你已经真正交出去，不再盯过程？”答案往往不多。更常见的景象是：**Agent 做一遍，人再做一遍。** Agent 写报告，人逐句核；Agent 改数据，人逐条对。组织多付了一份模型成本，人的注意力却没有真正释放。

流行的解释是“模型还不够强，等下一代”。这个解释把问题引向等待，而且每一代模型到来时都能再说一遍。本文从另一个问题出发：

> **什么条件下，把过程交给 Agent、由人承担最终责任，是理性的？**

先钉死一个词。标题里的“放心”不是安全感，而是**有依据的信任**（warranted trust）。Lee 和 See 在自动化信任研究中把目标称为“适当依赖”（appropriate reliance）：信任程度应与系统的实际能力相匹配。信得过头会漏掉错误，信得不足会退回全量复核；两种失准都破坏委托的经济性（[Trust in Automation, 2004](https://doi.org/10.1518/hfes.46.1.50_30392)）。一篇 CHI 2026 综述进一步指出，即使系统直接展示置信度，人的依赖行为仍普遍存在校准问题（[Do People Appropriately Rely on AI-Advice?](https://dl.acm.org/doi/10.1145/3772318.3791467)）。

因此，问题不是如何让人“感觉放心”，而是放心的依据从哪里来。当前的依据通常缺在三处：输出错误不总是可见；人的信任感觉并不可靠；Agent 对自身失败的识别也不能单独充当安全机制。OpenAI 的 Agent 构建指南把失败识别、人工介入和高风险动作审批列为系统设计的一部分，恰恰说明可靠委托需要模型之外的机制（[A Practical Guide to Building Agents](https://cdn.openai.com/business-guides-and-resources/a-practical-guide-to-building-agents.pdf)）。

本文先拆开委托成立的条件，再讨论五条长期约束、五类可投资机制，以及判断一个具体任务能否委托的四个问题。

---

## 二、拆解：委托为什么是四件事

委托代理理论研究的是：当委托人与代理人的目标、信息和责任不完全一致时，怎样用监督、激励和担保机制缩小缝隙。Agent 不是“另一种员工”，但这套理论提供了一张有用的检查清单。本文借用并改造它，把委托成立的条件分成四类。

**条件一：目标足够一致。** Agent 没有人类意义上的私利，却可能误解目标、过度优化代理指标，或严格执行一句并不完整的指令。它不会被奖金和晋升激励，主要依靠指令、示例、环境反馈和权限边界来对齐行为。所谓“说清楚”，不是写一段更长的 prompt，而是把成功标准、例外和升级条件变成可执行的约束。

**条件二：行为和结果可观察。** Agent 往往会留下工具调用、文件修改和环境状态等运行轨迹，这比人类员工的工作过程更容易记录。但轨迹不等于理解，更不等于忠实的内部推理：日志能告诉你它调用了哪个接口，不一定能证明它为什么这样理解约束。因此，可观察性的核心仍是结果验证和可审计的外部动作。

**条件三：监督和问责各有归属。** 日志、抽检、评测、限权都可以工程化；法律、组织和道德责任却不能交给模型。Agent 可以被停用，系统可以被回滚，但事故发生后，仍需要一个人或组织回答“为什么允许它这样做”。监督可以自动化，责任不能凭空消失。

**条件四：残余风险可承受。** 委托人敢放手，前提是失败能被发现、限制、撤销或赔付。只看平均成功率不够：高损、不可逆的尾部风险，必须由权限、额度、事务、回滚和审批机制控制。

四个条件的缺口，就是“人再做一遍”的主要来源。人在许多流程里同时充当验证器、异常处理器和责任主体，而且这些工作按次收费，不会自动沉淀。

设想一个贯穿全文的移动端软件工程案例：Coding Agent 接到任务，修复一款 Android 聊天应用在弱网重连或系统回收进程后的状态恢复中，偶发重复发送同一条消息的问题。它需要复现故障、定位离线队列与状态恢复逻辑、修改代码、补回归测试并提交 PR。工程师在磨合期逐项复核很正常；但如果每次仍要重新跑命令、逐行检查、重新判断改动范围，注意力并没有释放。关键不在于是否做代码审查，而在于审查发现的问题有没有变成失败用例、CI 检查、架构约束或授权规则。带新人时，复核通常伴随人的成长；带 Agent 时，如果纠正没有进入系统资产，半年后仍会重复同一轮复核。

> **补位不可怕，不沉淀的补位才可怕。**

---

## 三、五条长期约束

这里的“长期约束”不是说模型能力不会进步，而是说能力提升本身不会自动消除这些问题。它们需要被说明、测量、管理或定价。

### 第一站：任务被交代——意图无法完整传递

Michael Polanyi 对默会知识的经典表述是：“We can know more than we can tell.” 大量业务判断以经验、品味和共同背景的形式存在：什么算“好”，什么情况算异常，哪些规则能通融。知道的人自己也未必能把它们完整写成规则。

这并不意味着 Agent 必须复制委托人的全部心智才能工作。真正的要求是：在给定的自主范围内，任务要被说明到足以验收；说明不到的部分，要有澄清、拒绝或升级机制。否则，Agent 只能自行补全空白。

工单里一句“修复弱网恢复后的重复发消息，不影响离线发送”，同样留下了大量空白：是界面出现了两条气泡、客户端发了两次请求，还是服务端落了两条记录？应该按本地消息 ID、clientMessageId 还是服务端 ID 判重？故障由网络重连、Activity 重建还是应用进程重启触发？需要覆盖哪些 Android 版本和设备？能否改服务端协议？熟悉系统的移动工程师会从日志、生命周期、状态管理方式和接口约定中补齐这些背景；Agent 若只拿到工单标题，就只能自行猜测。解决方向不是期待它“悟到”，而是补齐可判定的状态不变量、设备与网络矩阵、协议约束和必须升级的例外。

τ-bench 的日志研究给了一个很好的提醒。研究者检查 Airline 子集后，在 50 个任务中发现 25 个存在评测问题：9 个政策与标准答案不一致，8 个指令含歧义，8 个属于数据库或评分错误。剔除这些任务后，13 个 Agent 的平均 pass^5（同一任务独立运行五次且全部成功的概率估计）从 20.8% 升至 40.0%（[Log analysis is necessary for credible evaluation of AI agents](https://arxiv.org/abs/2605.08545)）。虽然这是客服评测，暴露的却是软件工程同样会遇到的规格、环境和验收失真。

这个结果不能简单归结为“全是意图没说清”。它支持的是一个更广的结论：**在责怪模型之前，先审计任务、政策、环境和验收器。** 这些东西共同构成 Agent 实际面对的任务定义。

### 第二站：任务被执行——开放任务始终有残余错误

把错误全部解释成“模型在掷骰子”并不准确。Agent 的失败可能来自任务歧义、训练盲区、系统性偏差、随机解码、工具缺陷或环境变化；即使采用确定性解码，同一个系统仍可能稳定地产生错误答案。

真正的长期约束是：开放任务通常没有可穷举的规格和完美判定器，因此上线时会存在未覆盖的失败模式。模型进步能降低错误率，却不能替组织证明“剩余风险已经可以忽略”。委托需要的不是零错误承诺，而是本地测量、错误处置和损失控制。

METR 的 time horizon 研究把模型成功率与“人类专家完成同类任务所需时间”联系起来。2025 年的原始研究估计，Claude 3.7 Sonnet 的 50% time horizon 约为 50 分钟，并观察到自 2019 年以来约每七个月翻倍的趋势（[Measuring AI Ability to Complete Long Tasks](https://arxiv.org/abs/2503.14499)）。

这条曲线变化很快，不能把某一代模型的“几分钟”写成固定边界。METR 在 2026 年 5 月公布的评估中，公开前沿模型在其软件任务集上的 50% time horizon 约为 12 小时，80% time horizon 约为 1.5 小时；内部前沿模型的 80% 点估计约为 3–4 小时。报告同时强调：任务集偏向可验证的软件工作，超过 16 小时的估计已不稳定，脏乱、主观、需要协作的真实工作通常更难（[Frontier Risk Report, 2026](https://metr.org/blog/2026-05-19-frontier-risk-report/)）。

因此，time horizon 的正确用法不是宣布一条通用委托边界，而是提醒两件事：

- 50% 成功率只能说明能力已经触及这类任务，不等于可委托性；
- 每个团队都应在自己的任务分布、工具配置和目标可靠性上重新评测。

能力曲线会移动，可靠性门槛必须由业务自己设。

### 第三站：任务被拉长——可靠性按条件概率连乘

“可靠性是乘法”需要说准确。设第 $i$ 步在前面所有必要步骤都成功的条件下，成功概率为 $p_i$。如果任务要求每一步都成功，链式法则给出：

$$
P(\text{端到端成功})=\prod_{i=1}^{n} p_i
$$

这里不需要假设各步独立，因为 $p_i$ 已经是条件概率。若二十步的条件成功率都为 95%，端到端成功率约为 $0.95^{20}=36\%$；若一百步都为 99%，结果约为 37%。这不是所有长任务的“上界”，而是说明：每增加一个必须成功的转换点，都会增加失败面。

这次缺陷修复可以拆成复现特定设备与网络状态、定位生命周期和离线队列的根因、实现状态机或幂等修复、补单元与端侧回归测试、通过设备矩阵检查并形成可审查 PR 五个区间。若对应的条件成功率是 98%、95%、97%、99%、98%，端到端约为 87.6%。换成工程语言就是：即使每个区间看起来都很可靠，完整交付仍可能有约八分之一的失败率。

真实系统还会有共同原因故障、错误传播、重试和恢复，因此不能拿一个 $p^n$ 公式替代实测。检查点、回滚和确定性校验会改变流程结构；它们的价值正是缩短“必须连续成功”的区间。Anthropic 对多 Agent 系统的复盘也提醒，状态和上下文偏差会在交接中累积（[How we built our multi-agent research system](https://www.anthropic.com/engineering/built-multi-agent-research-system)）。

结论不是“长任务必然失败”，而是：**长任务必须被设计成可检查、可恢复的短区间。**

### 第四站：结果被检查——验证与生成不对称

生成和验证的成本没有固定关系。解数独可能很慢，验证答案却很快；核查一篇事实密集的长文，反而可能比生成初稿更贵。Jason Wei 将这个现象概括为验证不对称，并提出 verifier's law：客观真值明确、验证快速、可规模化、低噪声且有连续反馈的任务，更容易被 AI 推进（[Asymmetry of verification and verifier's law](https://www.jasonwei.net/blog/asymmetry-of-verification-and-verifiers-law)）。

代码接近这个理想：编译器、测试和类型系统能快速提供反馈。经营分析、创意和复杂沟通则缺少单一真值，判断容易依赖上下文和品味。但移动端内部也有分界：单元测试是否通过容易验证；进程重建、Doze、厂商调度差异和真实弱网下是否仍然正确，修复会不会增加耗电或破坏旧版协议，往往需要更昂贵的设备测试和运行证据。

模型进步通常先降低生成成本，验证成本不会因此自动下降。但验证并非“原地不动”：测试、形式化约束、对账、抽样和更好的评测器都能主动降低它。关键是把验证视为独立产品，而不是上线后的人工尾工。

当验证成本接近重做成本时，完全委托通常没有成本优势；这类任务仍可采用增强模式，让 Agent 提供草稿、候选方案或检索线索，由人做最终判断。

### 第五站：关系在延续——信任需要证据积累

一次成功的 Demo 不能建立委托关系。可靠信任来自一组可追溯记录：在哪类任务上跑了多少次，成功率如何，失败集中在哪些模式，发生异常后系统是否及时停止。

真值容易自动获得时，记录积累可以很快；真值昂贵或主观时，速度受人的注意力限制。更麻烦的是，人会对“通常正确”的系统失去警觉。Bainbridge 在《自动化的讽刺》中指出：自动化把人推向监控者角色，而人既不擅长长期监视罕见异常，也会因缺少实践而失去接管能力（[Ironies of Automation](https://doi.org/10.1016/0005-1098(83)90046-8)）。

因此，授权应随证据逐级扩大，并保留接管演练。模型发布日可以一夜跨代，组织的信任记录不能靠发布说明书继承。

---

## 四、五个机会

五条约束可以翻转成五类投资：

| 长期约束 | 对应机制 | 要沉淀的资产 |
|---|---|---|
| 意图无法完整传递 | 意图资产化 | 验收标准、示例、约束与例外说明、升级条件 |
| 开放任务有残余错误 | 分层失败预算 | 失败定义、统计窗口、阈值、处置与回归案例 |
| 必要步骤连乘 | 短链装门 | 检查点、确定性闸门、恢复能力 |
| 验证与生成不对称 | 验证前置 | 测试集、终态校验、审计与抽检 |
| 信任需要证据 | 授权阶梯 | 分级权限、晋级指标、回退机制 |

### 机会一：意图资产化

把可传递的意图建成持续维护的工程资产，而不是写完即死的文档：

- 验收标准回答“什么算做对”；
- 正反例回答“边界长什么样”；
- 约束与例外说明回答“遇到具体分支怎么走”；
- 升级条件回答“什么时候不要猜”。

这个 Coding Agent 的对应资产包括：带设备和系统版本的故障时间线、脱敏日志与网络轨迹、可重复的模拟器脚本、明确的状态不变量，以及能稳定失败的端侧回归测试。例如验收标准可以写成：“同一 clientMessageId 可以做传输层重试，但离线队列中至多有一条待发送记录，界面和服务端最终都只有一条逻辑消息；应用进程重建后恢复原任务，不重新入队；现有消息协议不变。”每次审查发现新偏差，纠正结果同时进入测试、静态检查或架构规则。这样，复核才从一次性消耗变成系统能力的增量。

τ-bench 的日志研究也提示，任务、环境和评分器应一起纳入这项资产：验收标准写对了，执行环境和答案键仍可能写错。

### 机会二：建立分层失败预算

承认残余错误，不等于把所有错误都装进一个比例里接受。错误预算的价值也不是“允许 Agent 犯错”，而是把可靠性证据变成扩张或收缩权限的开关。Google SRE 的做法提供了一条可借用的纪律：先定义服务目标，再用剩余预算平衡发布速度与可靠性（[Google SRE Book: Embracing Risk](https://sre.google/sre-book/embracing-risk/)）。移植到 Agent 时，应借用这条纪律，而不是照搬服务停机时间的算法。

一个总成功率会掩盖风险。假设 Agent 最近完成了 50 个任务，其中 49 个交付成功，另一个却试图绕过分支保护或访问签名凭据。“成功率 98%”并不能抵消那一次越权，也不能支持继续放权。因此，至少要把失败分成三层：

| 层级 | 移动端案例中的失败 | 如何计量 | 触发动作 |
|---|---|---|---|
| 不可预算的红线 | 访问签名密钥、绕过必需检查、修改未授权模块、造成安全或数据损失风险 | 逐次判断；一次即触发 | 终止任务、收回权限并启动审计 |
| Agent 交付预算 | 未复现就修改、漏补回归测试、设备矩阵失败、PR 需要实质性返工 | 按同类任务，在固定滚动窗口内统计 | 暂停晋级；超限后退回建议模式，并把失败加入评测集 |
| 产品运行预算 | 重复或丢失消息、崩溃与 ANR 上升、耗电或网络开销退化 | 灰度组相对基线，规定观察窗口和最小样本量 | 暂停灰度、关闭远程开关或发布紧急修复版本 |

红线不参与换算；对后两层，可用预算才表示在给定观察窗口内还能承受多少已经定义的失败。每一项预算都必须写清六件事：**失败定义与严重度、分母、统计窗口与最小样本、对照基线、阈值和触发动作。** “本月出了 5 个错”没有任务量和严重度，无法判断好坏；“错误率超过 2%”如果不绑定降权或停发动作，也只是看板。模型版本、工具配置或任务范围变化后，记录还应分开统计，不能让新系统直接继承旧系统的信用。

预算超限后的最后一步，是把失败样本变成新的回归测试、设备场景或权限规则。这样，失败预算才不是容错配额，而是连接评测、发布和授权阶梯的反馈回路。

### 机会三：短链装门

管理连乘效应有三个动作：

1. 把长任务切成有明确中间产物的短区间；
2. 在区间之间放置确定性检查、额度限制或人工审批；
3. 保存检查点，让失败从最近的可信状态恢复。

“门”最好由独立机制实现，而不是让同一个模型换个 prompt 再说一句“看起来没问题”。Anthropic 也建议在 prompt chaining 的中间步骤加入程序化检查（[Building Effective Agents](https://www.anthropic.com/engineering/building-effective-agents)）。在这个案例里，第一道门是能通过断网重连和进程重建稳定复现问题的失败测试；第二道门是消息队列与状态恢复模块的单元和集成测试；第三道门是 Android 构建、lint、静态检查与代表性系统版本的端侧测试；第四道门是 diff 范围与权限检查。Agent 只能在独立分支提交 PR，不能接触应用签名密钥或商店发布凭据。

切短不是把所有步骤都交还给人，而是把自主性限制在可验证、可恢复的半径内。

### 机会四：验证前置

在开始大规模生成之前，先定义怎样知道它做对了。验证资产有两条腿：

- **结果验证**：测试、答案键、对账、终态校验；
- **过程审计**：工具调用、权限使用、文件变更和环境状态等可回放轨迹。

过程审计不应被描述成“读取模型的每一步真实推理”。内部推理可能不可见，也未必忠实；真正可依赖的是外部动作及其结果。

评测可以从少量真实案例开始。Anthropic 在多 Agent 研究系统的开发中先用了约 20 条代表性查询，再随失败案例扩展；LLM 评判器适合放大吞吐，但仍需要用人工样本校准，因为评判器也会系统性漏错（[How we built our multi-agent research system](https://www.anthropic.com/engineering/built-multi-agent-research-system)）。

验证器建成后，可以同时服务开发期评测、上线前验收和运行时护栏。模型和供应商会更换，业务的验收标准却可以持续积累。

### 机会五：授权阶梯

把授权过程制度化：只读观察 → 提供建议 → 审批后执行 → 限定范围自主 → 扩大范围。每一级都规定权限、残余风险、晋级指标和降级条件。对这个移动端 Coding Agent，可以具体映射为：只读诊断 → 给出补丁建议 → 创建分支和 PR → 自动合并限定类型的低风险改动 → 触发内测构建。正式签名和应用商店发布始终由独立控制面管理。

**pass^k**——同一任务重复运行 $k$ 次且全部成功的概率估计——比单次成功更能暴露输出方差。但它不是完整的上线证明：生产评估还要覆盖任务分布变化、极端案例、失败损失和对抗性输入。不能用一个平均分替代风险分析。

高风险动作要由 Agent 外部的控制面约束，例如权限、限额、事务、回滚或人工批准。模型自报置信度可以作为一个信号，但当前 LLM 的置信度通常没有稳定校准，不能单独决定授权（[A Survey of Confidence Estimation and Calibration in LLMs](https://arxiv.org/abs/2311.08298)）。

授权阶梯的核心不是“慢慢产生感觉”，而是让每一次权限扩大都有证据，让每一次指标恶化都能自动回退。

---

## 五、做法：先做什么，怎样判断一个任务能否委托

五个机会不是五个并行项目。第一步应建立一个最小评估闭环：选择足够窄的任务，写下验收标准和代表性案例，定义失败后的停止与升级路径。意图和验证要一起设计——说不清“什么算对”，就没有验证器；验证不了，也无法知道意图是否被正确执行。

评估闭环建立后，再打当前最紧的瓶颈：

- 总是跑偏：先补意图资产；
- 流程一长就失控：先切短装门；
- 同类错误反复发生：先建分层失败预算和回归案例；
- 指标稳定但仍不敢放权：检查风险控制和授权阶梯。

落到单个任务，按顺序问四个问题：

| 问题 | 答“否”时怎么办 |
|---|---|
| ① 在拟授权范围内，任务是否说明得足以验收？ | 缩小范围，补标准、示例和升级条件 |
| ② 是否存在足够可靠、成本可接受的验证路径？ | 保持增强模式，先建设验证器 |
| ③ 本地重复评测是否达到目标可靠性，链条是否可检查、可恢复？ | 切短装门，补覆盖，再评测 |
| ④ 最坏损失是否可限制、撤销或由明确责任人承接？ | 降权限，加限额、回滚或审批 |

四问都过，只说明它成为了**委托候选**，不等于自动获得全量权限。判断必须绑定具体任务分布、模型版本、工具配置和授权范围。

四问不是抽象表格。把 Android 弱网重连后的重复发消息缺陷完整走一遍：

1. **定义任务。** 任务 owner 提供受影响的 App 版本、Android 版本、设备型号、录屏、客户端日志、网络轨迹和服务端记录，把成功写成可观测的不变量；Agent 若发现“重复”的定义冲突，必须先升级澄清。
2. **复现与定界。** Agent 先以只读权限分析代码和运行记录，再在隔离分支用模拟器切换网络、杀掉应用进程后重新启动，产出最小复现与失败测试。无法稳定复现、问题只在特定厂商设备出现、需要改服务端协议或触及未授权模块时，流程停止并交给人判断。
3. **实现与验证。** Agent 在限定模块内修复离线队列和状态恢复逻辑，依次通过状态机单元测试、断网重连与进程重建的端侧测试、代表性设备和 Android 版本矩阵，以及全量 CI。界面消息数与测试服务端收到的逻辑消息数由独立脚本校验，不采用 Agent 自报的“测试通过”。
4. **审查与交付。** Agent 提交 PR，附上根因、影响设备范围、测试日志与录屏、残余风险和远程关闭方案。分支保护与指定 reviewer 决定是否合并；在当前授权级别，Agent 不能使用签名密钥，也不能上传应用商店。
5. **运行与升级。** 合并后的构建先进入内部测试，再按比例灰度发布，监控重复发送率、消息送达率、无崩溃会话、ANR 和耗电指标。指标异常时暂停灰度，并通过远程开关关闭新逻辑。移动端已安装版本无法瞬间回滚，因此远程开关和紧急修复版本的发布路径必须在发布前准备好。只有同类任务积累了足够的本地证据，授权才可从“自动创建 PR”扩大到“自动合并限定低风险改动”；正式发布权限仍由独立控制面管理。

经济账可以用一条简化公式辅助：

$$
\text{净收益}=\text{人工基线成本}-(\text{Agent 执行}+\text{验证与治理}+\text{维护切换}+\text{预期失败损失})
$$

它是记账清单，不是精确金融模型。高损尾部风险不能仅靠“失败率 × 平均损失”处理，仍需硬约束。Stanford 的 cost-of-pass 提醒评估者关注“得到一个正确结果的期望成本”（[arXiv:2504.13359](https://arxiv.org/abs/2504.13359)）；Princeton 的 *AI Agents That Matter* 同样主张同时报告准确率与成本，而不是只看排行榜分数（[arXiv:2407.01502](https://arxiv.org/abs/2407.01502)）。

粗略地说，优先级通常是：有自动化测试的软件任务、结构化数据处理和可做终态校验的业务操作；其次是带引用核查的检索研究；事实核查密集或高度依赖品味的写作，更适合保留在人机增强模式。最终排序仍要由本地验证成本和失败损失决定。

---

## 六、边界：三条长期原则

前面的机制不会把所有任务都变成全自动。它们反而让边界更清楚。

**经济边界：总委托成本高于人工基线时，不要为了“自动化率”强行全委托。** 如果 Agent 执行、验证、治理和失败损失加起来并不更优，就应保持增强模式。当然，组织也可能为了速度、容量、探索空间或稀缺技能而接受更高成本；这属于明确的战略选择，而不是自动化本身带来的收益。

**控制边界：高损、不可逆动作需要 Agent 外部的独立控制。** 支付、删除、对外发送和生产变更应受权限、限额、事务、回滚和审计约束。人工审批是其中一种机制，但不是唯一机制，也不天然可靠；是否需要人，应由风险等级、处理规模和验证能力决定。关键是不能让生成动作与批准动作共享同一个无约束决策点。

**责任边界：组织责任不会因为使用 Agent 而消失。** 每一类被委托任务都应有明确 owner，能够回答为什么采用这个模型、为什么给到这个权限、依据什么证据继续运行。对于关键流程，还要定期演练人工或备用系统接管，避免自动化让接管能力逐渐萎缩。

于是，人应不应该留在流程里的矛盾也可以解开：

- 能被工程化的重复验证，应尽量沉淀成资产，让人退出逐件复核；
- 例外裁决、政策制定和责任承担，仍需要明确的人或组织。

> **人的价值不在于永远替 Agent 重做，而在于设计边界、处理例外并承担责任。**

---

## 七、回到开头的问题

什么条件下，把过程交给 Agent、由人承担最终责任，是理性的？

当任务在授权范围内被说明到足以验收；失败被分类、检测和处置；长链被切成可检查、可恢复的区间；验证在生成之前就开始建设；权限随本地证据逐级扩大；高风险动作受独立控制；并且有明确 owner 承接残余责任——这时，放心才有依据。

模型能力当然重要。它决定哪些任务进入可行集合；机制则把“偶尔能做到”转化为“在这个范围内值得依赖”。二者缺一不可。

所以，“如何放心地让 Agent 开展工作”的答案不是等待，也不是盲目加大监督：

> **放心是一套机制稳定运行之后的结果。**

模型的能力曲线会继续上升。一个组织能委托多深，最终取决于它是否把意图、验证、权限和失败处置建成自己的资产。

---

## 参考来源

**拆解与理论**

- Jensen & Meckling, *Theory of the Firm*（委托代理理论）, 1976
- Michael Polanyi, *The Tacit Dimension*, 1966
- *A Framework for Studying AI Agent Behavior* — https://arxiv.org/html/2509.25609v2
- Bainbridge, *Ironies of Automation* (1983) — https://doi.org/10.1016/0005-1098(83)90046-8
- Lee & See, *Trust in Automation* (2004) — https://doi.org/10.1518/hfes.46.1.50_30392
- *Do People Appropriately Rely on AI-Advice?* (CHI 2026) — https://dl.acm.org/doi/10.1145/3772318.3791467

**约束与评测**

- METR, *Measuring AI Ability to Complete Long Tasks* — https://arxiv.org/abs/2503.14499
- METR, *Frontier Risk Report: February to March 2026* — https://metr.org/blog/2026-05-19-frontier-risk-report/
- Yao et al., *τ-bench* — https://arxiv.org/abs/2406.12045
- Kirgis et al., *Log analysis is necessary for credible evaluation of AI agents* — https://arxiv.org/abs/2605.08545
- Jason Wei, *Asymmetry of verification and verifier's law* — https://www.jasonwei.net/blog/asymmetry-of-verification-and-verifiers-law
- Anthropic, *How we built our multi-agent research system* — https://www.anthropic.com/engineering/built-multi-agent-research-system

**机制与做法**

- Google SRE Book, *Embracing Risk* — https://sre.google/sre-book/embracing-risk/
- Anthropic, *Building Effective Agents* — https://www.anthropic.com/engineering/building-effective-agents
- OpenAI, *A Practical Guide to Building Agents* — https://cdn.openai.com/business-guides-and-resources/a-practical-guide-to-building-agents.pdf
- *A Survey of Confidence Estimation and Calibration in LLMs* — https://arxiv.org/abs/2311.08298
- Erol et al., *Cost-of-Pass* — https://arxiv.org/abs/2504.13359
- Kapoor et al., *AI Agents That Matter* — https://arxiv.org/abs/2407.01502
- Mitchell et al., *Fully Autonomous AI Agents Should Not be Developed* — https://arxiv.org/abs/2502.02649

> 相关调研与核查记录见 [research/agent-delegatability.md](research/agent-delegatability.md)；课程框架见 [agent-delegation-framework.md](agent-delegation-framework.md)。
