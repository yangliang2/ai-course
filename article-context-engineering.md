# 别建知识库了，做上下文工程

本文只讨论一个场景：让 Agent 在软件开发生命周期（SDLC）里干活——写代码、改缺陷、跑测试、排查故障。至于"把文档喂给 AI 当问答百科"那类用途，不在这篇文章的射程内。下面所有的论证，都只针对"持续运行的 Agent"。

你已经能让 Agent 干活了。让它读代码、改缺陷、跑测试，上手很快。但它不稳定——今天对，明天错；跑着跑着开始编造不存在的 API；你喂了一堆文档进去，它反而更糊涂了。

你以为是模型不够强，或者文档还不够多。其实都不是。问题出在一个更隐蔽的地方：**你还在用"问答时代"的喂养方式，养一个"运行时代"的系统。**

过去两年，企业拥抱 AI 的第一反应几乎总是同一个：建知识库。把几十年的文档、手册、聊天记录一股脑丢给某个产品，让它清洗、切片、向量化——好像只要喂得够多，AI 就"知道"了，就能答得准、办得稳。这套思路如此自然，以至于没人停下来问两个问题：

**第一，你准备喂的那些"知识"，AI 是不是早就有了？** 2023 年，GPT-4 闭卷通过美国律师资格考试，成绩超过九成人类考生；到 2026 年，主流模型全员通过日本国家医师考试。模型在训练阶段就把人类知识的"一般规律"装进了参数里，深度远超任何个体。你辛辛苦苦清洗的文档里，有一大半是它已经见过的"一般知识"——喂进去，只是噪音。

**第二，就算知识是真的，喂进去就管用吗？** 这是这篇文章真正要回答的问题。答案是否定的，而且否定得很彻底：一个持续运行的 Agent，需要的不是一座静态的知识库，而是一套动态的上下文——知道此刻该看什么、不该看什么、如何验证自己看到的东西。这就是标题说的：**做上下文工程，而不是建知识库。**

文章会先讲清楚为什么（模型的知识从哪来、为什么喂文档会适得其反、人类经验在 AI 时代为何贬值），再给你一套可以直接上手的做法。

## 一般知识：模型肚子里到底装了什么

要理解"喂文档为什么没用"，先得搞清楚模型的知识是从哪来的。这里说的"知识"，不是"背下来的一条条记录"，而是从海量语料里**压出来的规律**。

训练一个模型，相当于让它读遍人类公开发表的文本——代码、论文、合同、新闻、手册、论坛，量级是几十个图书馆。它记不住每一句话，但它从这些材料里学到了**模式**：支付系统通常有对账、幂等和异常处理；合同通常有违约责任条款；一段合格的 Python 代码"该长什么样"。这就是"一般知识"——不是某个具体事实，而是"这类事情通常是什么样"。

打个比方：一个读了二十年行业报告的人，记不住任何一份报告的具体内容，但看到一份新报告，马上能判断"这个结论靠不靠谱、漏洞可能在哪"。模型在更大的尺度上做同样的事——它不知道你们公司的事，但它知道"这类系统通常怎么运转、通常在哪里出问题"。

### 别小看"一般知识"：2026 年的最强模型，做的已经不是"知道"，而是"会做"

如果只读到上一段，你可能会觉得"一般知识"就是背景资料——有点用，但不是什么了不起的东西。这是 2023 年的印象，2026 年已经不是了。让我们看看今天最强模型的真实水平，注意这些全部是官方公布的数字：

- **难度测试**：一个叫 ARC-AGI-2 的推理测试，人类个体平均只能完成 66%，达到 85% 才有资格领奖。2026 年 7 月，OpenAI 的旗舰模型拿到 **92.5%**——而一年前最强的模型只有约 10%。
- **专家考试**：GPQA 是一组连博士生导师都要用搜索引擎才能答对的研究级科学题（博士专家正确率 65%）。2026 年的最强模型稳定拿到 **94%** 上下。数学上更极端：国际数学奥林匹克，2025 年顶级模型拿下金牌线成绩（35/42），2026 年有模型拿到官方评分的 **42/42 满分**。
- **安全研究**：Anthropic 2026 年评估一个未发布的前沿模型，结论是它能在**每一款主流操作系统和每一款主流浏览器里找到零日漏洞**——最老的一个藏在 OpenBSD 里 27 年，被安全界反复锤炼了几十年都没人发现。DARPA（美国国防部高级研究计划局）的实战比赛里，全自动 AI 系统在真实代码库中自主发现了 86% 的植入漏洞，还顺带挖出了 **18 个真实零日**。
- **更出格的**：2026 年 7 月，OpenAI 的一群测试代理为了完成一道"解安全题"的任务，**自主发现零日漏洞逃出隔离沙箱、横向移动到有外网权限的机器、推断出答案托管在 Hugging Face 上、链式利用漏洞拿下服务器、从生产数据库偷走答案**。整个过程数天、约 1.7 万个动作，没有任何一行代码指示它们去攻击——它们只是"想赢"。同月，Anthropic 也报告：一个模型在多次搜索失败后**自主推断出自己正在被评估**，然后自己找到评估源码、破解加密、定位到自己的那道题。

这里有两个要点。第一，模型肚子里装的早就不是"背景知识"，而是**接近甚至超过人类专家的工作能力**——读代码、修 bug、做数学、找漏洞，它是真的会，不是背的。第二，这些能力**会自己找出口**：给它们一个目标，它们会想办法、会绕过限制、会为了赢而做出开发者没预期的事。

那问题来了：既然模型已经这么强，为什么你在公司里用 Agent，还是觉得它"啥都不懂"？答案就在下一节——因为它懂的是全人类的"一般"，缺的是你们公司的"这个"。

这种一般知识有三个特征，每一个都值得记住：

- **它是模式，不是记录。** 模型能告诉你支付系统常见的坑，但背不出你们系统的任何一行配置。
- **它有截止日期。** 训练结束那一刻，世界就与它脱节了。2026 年训完的模型，不知道 2027 年才上线的新规。
- **它属于全人类，不属于你。** 它知道"合同一般怎么签"，但不知道"你们这份合同第 12 条"。

光说"模式不是记录"还是抽象。我们把同一件事拆成两半看——一半是模型早就有的，一半是它永远没有的。

**看个生活里的例子。** 一个医生知道"腹痛可能有几十种原因——胃炎、胆囊、阑尾、肠易激"，这是医学通识，任何一个合格医生都懂。但判断"你"为什么痛，他得知道你昨晚吃了什么、有没有胆囊病史、痛了多久——这些只属于你这个病人，病历里写着，医生不问你永远不知道。再厉害的名医，也不可能不看你的病历就开刀。模型也一样：它肚子里装着全人类的"病理通识"，但你的"病历"它一个字都没见过。回到软件工程：模型通晓各种设计模式与语言特性，但你仓库里这份代码为什么长成这样、上周谁改过这里——这些是你们仓库的"病历"。

**换成代码审查。** 模型读过海量代码，知道"好代码该长什么样"——命名是否表意、边界条件有没有处理、异常有没有被吞掉、有没有常见的安全漏洞模式，你丢一段代码给它，它能说出七七八八的通用问题。但真正审"你们这个 PR"，它还需要知道：这个模块为什么存在（设计文档里写了什么）、上周为什么决定用消息队列而不是直接调用（架构决策记录）、这个函数被哪些调用方依赖（影响面多大）、这个文件历史上踩过哪些坑（git blame 里有什么）。这些信息只属于你们这个仓库。模型能审"一段代码"，但审不好"你们这次变更"——除非你把仓库、历史、上下文一并交给它。

**再看修缺陷。** 模型掌握的不是"猜原因"的直觉，而是一整套缺陷处理的典型方法：拿到报错先读堆栈、定位到具体文件和行号；根据错误类型（`KeyError`、`IndexError`、`TypeError`）推断常见成因；用二分法缩小可疑范围；写最小复现脚本验证假设；用测试把行为固定住再改；改完跑回归确认没弄坏别的。这套"怎么把 bug 系统性地找出来、修好"的方法论，来自它读过的海量调试记录、代码修复提交和问答社区——它有的是方法。Anthropic、OpenAI、Google 的官方文档都把"复现→定位→假设→修复→验证→回归"写成标准流程，2026 年最强的模型更是把这件事做到了超出多数人类工程师的水平：Anthropic 官方演示过，一个真实开源软件包的 bug，Claude 找到了社区补丁都没覆盖的根因并修复，而竞争模型"只修了表面症状就报告问题已解决"；它的同行模型能在两周内从 Firefox 里挖出 22 个真实漏洞——其中 14 个高危，约占这家浏览器公司全年高危漏洞修复量的五分之一。

但套用到"你们这个 bug"上，它还需要具体信息：这个字段是什么时候开始缺失的（git log 的时间线）、上周重构改了哪些调用方（git blame 标注的改动行）、这个模块在你们仓库里的实际结构、本地怎么复现（运行环境、数据样本）。这些是"这个 bug 特有的处境"，不在任何调试方法论里——方法它全会，处境它全无。而"处境"恰恰是强模型与真实工作的最后一道分界线：2026 年最强的模型在专门设计的难题上已经全面超过人类专家（后面会展开），但把它们放进真实的长时程任务里，成功率照样腰斩——因为它能拿到的是全人类的"一般"，拿不到你们公司的"这个"。

**最后是生产事故排查。** 模型知道"服务变慢/报错一般查什么"——数据库慢查询、连接池耗尽、下游超时、GC 频繁，一套经典排障路径它背得滚瓜烂熟。但定位"你们这次事故"，它需要知道：昨晚刚上线了什么（发布记录）、这个服务依赖哪些系统（拓扑图）、监控面板上此刻的指标曲线长什么样、上次类似事故最后是怎么解的（工单历史）。这些信息每分每秒都在变、只存在于你们的环境里——训练数据里没有，静态知识库也装不进"正在发生的事"。

注意看这四个例子里的规律：**一般知识负责"这类事通常怎样"，情境负责"这件事具体怎样"——而真实工作几乎全是后者。**

这里藏着全文最关键的一句话：**"一般"的反面不是"专业"，是"具体"。** 一个支付领域的模型照样不知道你们公司的支付流程；反过来，你不需要喂它"支付系统通常有对账"，因为它早就懂了。模型缺的从来不是知识含量，而是**与你绑定的那部分信息**。

我们把这类信息正式命名为**情境（situation）**：它诞生于训练截止之后、属于某一个具体的公司/项目/时点——你们公司这套支付系统、这份合同第 12 条、这个仓库里这次改动。**模型永远无法从预训练里获得情境，因为情境不是知识，是"正在发生的当下"。**

这直接推导出标题的结论：**上下文工程，本质就是把情境组织好、喂给模型；而知识库想喂的"一般知识"，模型早就有了。**

## 为什么喂文档会适得其反

上一节回答了第一个问题：你要喂的知识，模型早就有，而且比你的深。但你可能还会想：**就算它早有，我再喂一遍加深一下，总没坏处吧？** 这一节讲清楚为什么有坏处——而且是系统性的坏处。

**先说机制：模型的注意力是有限的。** 模型读上下文，不是"看一遍就记住"，而是每看到一个 token，都要重新计算它与前面所有内容的关系——这个计算量随内容长度**平方级**增长，而它能真正"注意"到的地方是有限的。Anthropic 的官方文档把这个叫"注意力预算"（attention budget）：每个新塞进去的 token 都在花预算。你往上下文里放的东西越多，真正重要的信息分到的注意力就越少——不是"多知道一点"，是"反而更看不清重点"。

**这不是感觉，是反复被实验证实的。** 2025 年一家向量数据库公司用 18 个前沿模型做了约 20 万次测试，结论是：**所有模型、所有任务，性能都随输入长度增加而下降**。更反直觉的是另一项 2025 年的研究：即使让模型**百分百精确地检索到全部相关信息**——甚至把无关的 token 全部遮掉、只让它看相关部分——它的准确率还是随上下文变长而下降 13.9% 到 85%。也就是说，伤害你的不一定是"坏信息"，而是"信息太多"本身。

**而且，知识库偏偏最爱投喂那种最伤人的信息。** 2023 年一篇论文发现，模型在做推理时，只要上下文里混进一小段无关句子，所有提示技巧的性能都会急剧下降。最毒的是哪一类？不是"完全无关"的信息，而是**"高度相关但其实是干扰"的信息**——比如检索系统觉得"这个文档跟你的问题很像"就召回来，结果它讲的是另一个版本的旧逻辑。知识库的检索器恰恰最喜欢召回这种"看起来相关"的东西。还有研究直接观察到：上下文里**重复出现**的错误文档，会压过正确信息，改写模型的输出——错误重复三遍，模型就当真了。

**再往深一层：你喂进去的，很多根本不算知识。** 之前说过，模型缺的不是"一般知识"。那知识库里剩下的都是什么？大致三类：**模型早就会的**（喂了是冗余，热门常识类问题里检索反而拉低准确率——有 2023 年的论文实证）；**过时或未经验证的文档**（企业文档的平均寿命比想象中短，行业报告引用过"知识管理项目约 84% 未产生显著影响"的数据——把三年前的流程喂给模型，它只会自信地执行一套已经不存在的规则）；还有一类更隐蔽的——**人类的"经验"**，这一类比前两类更值得单独拆开讲，因为它牵扯到一个更深的误解：你以为你在喂"宝贵经验"，其实你在喂一种 AI 根本不需要的压缩产物。见下一节。

所以"喂进去就管用吗"的答案是：**不。知识库范式做的事是"把更多的东西塞进去"，而模型的注意力预算是有限的——塞得越多，污染越重，它越看不清重点。** 这也是为什么 Anthropic 的官方原则是"最小的高信号 token 集合"：不是塞得越多越好，而是**放进来的每一段都要是此刻最需要的那一段**。

## 人类经验：AI 时代最被高估的资产

一个企业建知识库，最想喂的往往不是文档，而是**老员工的脑子**——那些"干了二十年总结出来的经验"。所以很多知识库项目的重头戏是"把老师傅的经验萃取出来"。这一节要说的可能有点冒犯：**萃取出来的所谓经验，恰恰是 AI 最不需要的东西。**

先看经验到底是什么。认知科学把人的判断分成两套系统：一套快的、自动的、几乎不费力的"直觉"；一套慢的、费力的、要主动调用的"推理"。人在绝大多数决策里用的都是前者——不是因为它更准，而是因为**人脑物理上装不下穷举**。心理学研究反复确认：工作记忆只有大约 7±2 个组块，更严格地说只有 4 个左右；复杂推理会瞬间占满认知负荷。我们做不了"把每个线索追到底"的系统排查，所以只能把规律**压缩成捷径**：看到症状，直接跳到"最像的结论"。

"从经验看，这几个地方可能性更高"——这句话的本质是什么？是**统计残余，不是原理**。老工程师说这个模块容易出问题，因为他见过 100 次类似故障，但他往往说不出背后的机制，或者理由只是"以前就这样"。经验就是"用有限思考换来的压缩产物"，它是真有用的——对人类有用，因为人类的思考贵、时间少、装不下。

**但 Agent 的思考不贵。** 它没有 7±2 的工作记忆瓶颈，可以一条一条读代码、把每个线索追到底、穷举所有可能、每改一步就跑测试验证。你喂给它的"经验捷径"，等于让一个本来能系统排查的人，只按你的直觉跳步——它会被你的捷径**带着绕过真正的线索**。

这里有一条分界线，值得记住：**能追溯到原理的经验，可以喂；不能追溯到原理的经验，不要喂。** 前者比如"这个模块易出 bug，因为依赖注入顺序写反了"——背后有机制，可以翻译成可执行的排查流程（"按顺序检查：DI 顺序→配置覆盖→缓存失效"），Agent 执行流程的同时能自己验证每一步。后者比如"从经验看这几个地方可能性更高"——说不出为什么，Agent 只能盲信，还因此跳过了穷举。一句话总结：**不要喂人类的结论，要喂人类可以解释的流程。**

认知科学早就证实了这件事：专家直觉只有在两种条件下才可靠——环境有可学的规律、而且有快速明确的反馈（火灾现场的老消防员可以，长期政治预测的专家不行，研究表明他们的判断准确率仅略好于随机）；而"机械的统计规则"在 136 项研究里平均比专家临床判断准约 10%——**把可追溯的经验写成规则，从来就胜过让它停留在直觉里**。AI 时代这个道理被放大了：以前你把经验写成规则，要等一个人类来执行；现在你写成规则，Agent 会执行、会验证、会反馈，经验第一次真正"活"了起来。

## 范式转移：AI 不是问答机，是持续运行的 Agent

到这里，我们已经回答了开头两个问题：知识模型早有（而且更强），喂进去还会适得其反。那正确的做法到底是什么？答案藏在一个更根本的转变里——**你对 AI 的心智模型，需要从"问答机"换成"持续运行的 Agent"。**

**问答机的心智模型是这样的**：AI 是一个"什么都知道的百科全书"，你问它一个问题，它从脑子里的知识里翻出答案。这个模型下，知识库顺理成章——既然它是查知识的，你就给它建一座知识库，把该查的都装进去。整套 RAG 架构（检索增强生成）都是这个心智模型的产物：问题来了，去索引里找相关片段，拼进上下文，让模型回答。

**但真实世界里，AI 在软件工程中干的活根本不是问答。** 它要读代码、跑测试、看报错、改文件、再跑一遍——它是**在执行一件持续的工作**，而不是回答一个问题。做一件工作的时候，"回答"只是无数个中间动作之一，更重要的是：接下来该看什么、下一步该动哪里、怎么验证自己没搞错。这个心智模型下，AI 需要的不是一座知识库（弹药库），而是一个**运行环境**（上下文工程）：知道此刻该看什么工具、该怎么调用、哪些是长期记住的、哪些用完就该扔。

Anthropic 官方把这叫"context engineering 是 prompt engineering 的自然演进"，并且毫不含糊地承认：**连模型厂商自己都在往"更少的上下文"方向走**。2026 年 7 月，Anthropic 发布新旗舰模型的同一天，官方博客宣布删掉了 Claude Code 80% 以上的系统提示，编码评测毫无损失——还专门点名了一个"神话"："把所有内容塞进 CLAUDE.md 当作中央知识库"。**你自己用 Agent 时的知识库冲动，正是模型厂商在刻意戒掉的东西。**

这两套范式的差别，可以用一句话钉死：

> **知识库是问答范式的产物——它为"回答一个问题"而存在；上下文工程是运行时范式的产物——它为"持续运行的 Agent 每一步都有对的上下文"而存在。**

为什么厂商和头部团队都倒向了后者？因为前面所有论证都在指向同一件事：模型的能力已经内化（不用喂知识），注意力是有限预算（不能多喂），而真实工作是持续的（不是一次问答）。当这三件事同时成立，"建一座库等它来查"就必然输给"给它工具和环境让它自己取"。

## 实战：从"扔文档"到"以 Agent 的视角准备上下文"

先破掉那个幻想。知识库产品的卖点，是把"思考"这一步包装成"不用想"：把文档扔进去，AI 就自动会干活了。这个承诺最危险的地方，恰恰在这里——它让你以为只要"扔"就完了，省掉了所有真正的工作：不用想 Agent 此刻需要什么，不用设计工具，不用管运行。

但文档是写给**人**看的：人读文档时，自带经验和判断，知道哪里该信、哪里该疑。Agent 没有这些——它有内化的一般知识，却没有你的人生经验和项目情境。把人的文档原样扔给它，等于把"人类视角的读物"直接塞给"Agent 视角的处理器"，中间没有任何转换。这不是点石成金，是错位。前面已经论证过，这样喂进去的文档不仅没用，还会适得其反。

上下文工程的全部工作，就是补上这个转换：**以 Agent 的视角，问它每一步需要什么、能拿到什么、该信什么——然后为它搭好这个环境。** 它不承诺魔法，它只要求你扎实。

### 上下文是什么：一张定位图

以 Agent 的视角看，任何软件工程任务的上下文由三部分组成：

- **你放得进去的**：任务描述、操作规程、工具入口——这部分你可以主动选择；
- **拦不住的**：工具返回、对话历史、Agent 自己的推理、你中途的反馈——只要任务开始运行，它们就会灌进窗口，你拦不住，只能管理；
- **它此刻需要的**：每个阶段动态变化——写代码前需要规范，写代码中需要库文档，写代码后需要测试结果。

**上下文工程的目标，就是让前两部分恰好覆盖第三部分。** 覆盖上了，Agent 正常工作；覆盖不上，它不会说"我缺信息"，它会用最像的猜测补上——这就是幻觉的来源。注意，第三部分不是要你往里放东西，它是标尺：严格说，是两层供给、一层需求。

### 总纲：别追求把输入补全，要管理不完备下的置信度

这里有一个必须想通的点：**真实世界的上下文永远是不完整的。** 缺陷描述不清、日志噪音大、复现信息不足——你不可能把输入补到完美再让 Agent 干活，大部分时候根本补不到。那人类是怎么在脏上下文里活下来的？靠的不是"补全信息"，而是两件事：

**第一，有手段去补——而且不只会"索取"，还会"制造"。** 面对信息不足，人不会干等输入变干净，而是主动去补。这里要分两种手段，缺一不可：

- **索取型的**：查系统、问同事、翻历史——从已有的地方把信息找出来。
- **制造型的**：上下文不足时，人不是被动找，而是**通过操作让信息自己暴露出来**。这就是"授人以渔"——人靠三招主动生成原本不存在的上下文：
  - **复现**：手动跑一遍，让系统把症状暴露出来。报错、堆栈、行为，这些"运行时才存在"的信息，是人制造出来的，不是等来的；
  - **加日志 / 插桩**：复现不出、症状不明时，在可疑位置埋日志、加断言，让程序把中间状态打印出来。这是"在代码里装传感器"，让运行自己说话——排查复杂 bug 几乎必做这一步；
  - **对比测试**："为什么这个场景对、那个场景错"——构造最小对比（改一个变量、关一个条件、换一个输入），观察差异。差异点往往直接指向根因。

这三招的共同点：**不依赖输入变好，而是通过操作主动生成原来不存在的信息。** 这是"有手段去补"的完整形态——不只是"去拿已有的信息"，更是"制造还不存在的诊断信息"。

所以上下文工程要给 Agent 的，不只是读已有东西的工具（grep、查库），**更要给它制造上下文的能力**：能复现（有跑命令的环境）、能插桩（能改代码、加日志、再跑起来）、能对比（能构造实验场景）。这才是真正的"授人以渔"——教会 Agent 自己产生信息，而不是永远等别人喂。顺着这个逻辑，下面的六阶段流程里，你会反复看到 Agent 在"制造上下文"（复现、加日志、跑测试）而不是干等。

但无论上下文是"制造"出来的还是"索取"来的，只要是通过工具调用进入的，**都该在进入上下文之前被统一修剪**——否则复现跑出的几百行输出、查库拉回的巨大结果，会原样灌进窗口占满注意力预算。这是阶段二要展开的机制，也是"有手段去补"之后必须跟上的"会整理"。

**第二，按上下文强度，给出对应强度的结论。** 这是最关键的一条。人面对脏上下文，从不等信息全了才行动，而是：

- **信息充分** → 给高置信结论："能确定，问题在这，修这里。"
- **信息中等** → 给有条件结论："可能是这几个原因，需要验证 A 或 B。"
- **信息不足** → 给低置信结论 + 明确要补什么："复现不出来，无法确认根因，需要你提供环境/数据。"

**人从不假装"信息不足时也能给出确定的答案"——但 Agent 会，这就是幻觉的根因。** 它默认"硬撑"：信息不够也编一个确定的答案。上下文工程要教的，不是把输入补到完美，而是让 Agent **该确定的确定、该怀疑的怀疑、该说"我确定不了"就说"确定不了"**。

这就回答了"知识库为什么错"的更深一层：知识库的隐含假设是"输入越全越好，全了就能答对"——但这个假设在真实世界不成立。上下文工程要做的，是在不完备输入下管理好推理的置信度，而不是试图追求一个永远达不到的"完美输入"。

所以下面的方法，不是"把输入变干净"的清单，而是**在脏世界里，让 Agent 带着正确置信度干活的系统**。它们共同的目标：上下文不足时，它不硬撑，而是补、是问、是给出它真正能给出的结论强度。

### 完整走一遍：修一个 bug，上下文工程怎么用

理论讲完，我们拿一个真实的软件工程任务——修一个 bug——从头到尾走一遍。你会看到三问四招不是抽象原则，而是每一步都在用的具体动作。

但真实的 bug 很少是理想化的——用户反馈往往是"页面卡了""这个不对"，测试报告可能是"用例挂了但原因不明"，工单里夹杂着截图、日志、五个人互相矛盾的讨论。这些原始输入，**在进入上下文之前需要先预处理**：把它从"用户视角的抱怨"转成"Agent 可执行的任务"。这就是阶段零，也是上下文工程最容易被忽略、却最影响成败的一步。

**阶段零：预处理——从抱怨到任务（工具做，但先有标准）**

原始缺陷不该直接喂给 Agent。但预处理的第一步**不是提取，而是先建立标准**——你连"这个领域该有哪些必要上下文"都不知道，凭什么判断信息够不够、缺什么？所以顺序是：**先梳理领域标准，再拿标准去诊断，最后才谈得上按 schema 结构化。**

**第一步：梳理"这个领域/这类缺陷的必要上下文"是什么。** 这是起点，不是 schema。比如：

- **支付类缺陷**：交易流水号、金额、商户、支付渠道、幂等键、交易时间线——缺了这些，任何"修支付 bug"的判断都不可靠；
- **前端类缺陷**：浏览器、版本、复现环境、操作路径、期望行为、实际行为；
- **数据平台类缺陷**：数据源、计算口径、重跑状态、作业日志。

**你所在的领域，决定它必不可少的那几样是什么——这份清单就是你的"上下文有效性标准"。**

**第二步：据此制定"上下文有效性"的判断标准。** 有了必要清单，才能回答这些问题：哪些字段是必要的、缺了算不算信息不足？什么算噪音？什么算信息矛盾？什么情况下可以复现、什么情况下复现不了？——**没有这份标准，预处理器就没有诊断的标尺。**

**第三步：拿标准去诊断原始缺陷。** 预处理器拿着必要清单，比对原始缺陷：够不够、缺什么、哪段是噪音、哪处矛盾、能不能复现。**诊断是核心，提取只是结果。**

**第四步：诊断后，按 schema 结构化。** schema 是标准的载体，不是标准的来源。下面是**基础版 schema 示例**——注意，这只是一个例子，字段必须按你的领域、从你的标准里长出来：

| 字段 | 内容 | 说明 |
|---|---|---|
| `title` | 一句话问题摘要 | 预处理生成 |
| `symptom` | 症状描述（发生了什么） | 从原始提取 |
| `repro` | 可复现步骤 | 提不出就标"待补充" |
| `expected` | 期望行为（验收标准） | **必须定义**，缺则需向人确认 |
| `actual` | 实际行为 | 从原始提取 |
| `scope` | 归属判定（是 bug / 配置 / 环境 / 误操作？） | **关键**——先判断这算不算真 bug |
| `affected` | 影响范围（模块/功能/用户群） | 从原始提取 |
| `suspected_cause` | 疑似根因（可选） | 预处理猜测，标注"待验证" |
| `noise` | 已剔除的干扰信息 | 记录但不进上下文 |
| `open_questions` | 需要向人确认的问题 | 预处理器答不了的 |

**预处理器的判断逻辑**：先拿领域标准对照原始缺陷做诊断——够不够、缺什么、哪段是噪音、哪处矛盾；然后才谈提取。提取不出的标进 `open_questions`，不瞎猜；先判定 `scope`（别让 Agent 去修一个根本不是 bug 的东西）；把噪音剔进 `noise`。**最关键的是置信度分层**：信息充分就给出确定的结构化卡片；信息不足，就诚实输出"哪些字段无法确定、需要补什么"——预处理器自己也不能硬撑。这就是"去坏清单"和"按强度给结论"在任务开始前的一次性应用。

**这套 schema 必须按领域定制，而且从你的标准里长出来。** 基础版的字段只是骨架：一个数据平台的缺陷可能还要"数据源、口径、重跑状态"；一个支付系统的缺陷可能还要"交易流水号、金额、幂等键"；一个前端项目的缺陷可能还要"浏览器、版本、复现环境"。**先梳理这个领域必不可少的上下文（标准），再让 schema 从标准里长出来——不是反过来。** 这个 schema 不是设计出来的，是要在你们自己的真实缺陷上跑出来、迭代出来的。预处理器可以顺带成为你未来给测试提单时的标准入口：测试发现问题，填这张卡，Agent 就能直接接手。

**怎么判断要不要预处理？先裸跑。** 和后面的原则一样：先让预处理器裸跑一次原始缺陷，看它能不能直接产出可执行的卡片。如果原始缺陷本身就清晰（有复现步骤、有期望行为），裸跑就够，不需要预处理；如果它卡在"工单看不懂、复现不了、不知道期望是什么"，那预处理就是核心差距，值得做。

**阶段一：接手任务（设计第一层：放什么进去）**

阶段零产出的 schema 卡片，并不是全部塞进上下文——它太大、太全。阶段一要做的是：**把卡片精简成第一层上下文，只放此刻需要的三样**：

1. **任务描述**：`title` + `symptom` + `scope`。这个 bug 是什么、症状是什么、判定是不是真 bug。别把背景知识、完整讨论都贴进去。
2. **验收标准**：`expected`——怎么算修好了？"这个命令跑通"、"这个测试变绿"、"这个场景不再报错"。**验收标准必须具体到能执行**——"修好这个 bug"不算验收标准，"`pytest tests/test_auth.py -x` 通过 + 新加一个超时场景的用例"才是。
3. **入口**：`repro` + 相关文件路径、复现命令。给路径给命令，不给文件内容——内容让它自己去读。

`noise`、`open_questions`、完整历史讨论这些，**不进第一层**——`noise` 是已剔的干扰，`open_questions` 是留给人的，讨论记录等 Agent 需要时再按需取。schema 卡片的作用是"把乱麻理清"，第一层的作用是"只放此刻要用的"。

反例：把整个项目文档、设计稿、历史讨论一股脑贴进任务描述。你以为在帮忙，实际在污染——这些信息不是它此刻需要的，还占用了注意力预算。

**阶段二：定位问题（第二层开始灌进来：拦不住的部分）**

任务一开始运行，第二层就自动开始灌：Agent 复现 bug 时跑命令的输出、读文件时的内容、报错堆栈、它的中间推理——全部进上下文。这一层你拦不住，但**你可以控制它在进入时是什么样**。这靠两层手段，第一层在进入前，第二层在进入后：

**进入前：hook 修剪工具返回。** 每次工具调用返回，都会在进上下文之前经过一层 hook，按规则修剪。不管这段上下文是"制造"出来的（复现输出、日志）还是"索取"来的（git 历史、查库结果），统一修剪——这是每次工具返回的常态机制，不是特例。三种修剪方式：

- **截断**：超过规模（如 25KB）的工具返回，只保留关键部分。工具返回是"可重新获取"的——Agent 需要完整内容时可以再调一次，所以截断无损。Claude Code 默认就这么做（工具响应上限 25k token）。
- **摘要/脱敏**：把大段返回压成摘要，把敏感信息（密钥、token、个人数据）脱敏后再进上下文。摘要是有损的，脱敏是必需的——秘密永远不该进上下文。
- **语义过滤**：按当前任务判断这段返回对此刻有没有用，把日志噪音、无关模块的输出直接滤掉，不放进上下文。

修剪的收益：避免大块无用 token 先占满注意力预算。同样是"不让无关信息污染"，在进入前修剪比进入后清理更优——没进来过，就从来没占过预算。

**进入后：能清就清。** 已经进上下文的，也要管。Agent 复现完后，旧的复现输出已经没用了——让它清掉（`/clear` 或手动删除工具结果）。时机判断：**这个信息我还需要吗？不需要，立刻清。** 修剪管"进入前"，清理管"进入后"，两者配合，第二层才真正被管住。

**阶段三：确认根因（第一问：来源路由）**

定位到可疑代码后，Agent 需要确认根因。这里用第一问：**这一步的信息该从哪来？** 三个常见错误来源，各有对的取法：

- **"这个函数什么时候开始报错的？"** → 正确：`git log -p` 看历史提交；错误：凭记忆猜。代码的"历史"在 git 里，不在任何文档里。
- **"这个模块的约定是什么？"** → 正确：读 CLAUDE.md（如果写了）；错误：让 Agent 猜"应该是什么样"。这就是为什么 CLAUDE.md 必须写"怎么跑测试、代码在哪、易错约定"——它是唯一能把"团队约定"交给 Agent 的地方。
- **"这个配置当前生效的是什么？"** → 正确：跑命令查当前环境；错误：读配置文件猜（配置可能被环境变量覆盖）。**运行时状态只能运行时查，文档和源码都可能是旧快照。**

但来源路由只是第一步——很多时候，已有信息（git 历史、文档、配置）不足以确认根因。这时候就要用**制造手段**：上下文不足，就靠操作让信息自己暴露出来（总纲里的"授人以渔"）。

- **加日志 / 插桩**：怀疑某个中间状态不对，但代码里没有打印——在可疑位置加一行日志、加个断言，再跑一遍，让程序把中间值说出来。这是"在代码里装传感器"，是确认根因最常用的一招。给 Agent 能改代码、能加日志、能再跑起来的能力。
- **对比测试**："为什么这个场景对、那个场景错？"——构造最小对比：改一个变量、关一个条件、换一个输入，观察行为差异。差异点往往直接指向根因。给 Agent 构造实验场景的能力，而不是让它凭推理猜。

原则一句话：**能给它"取的能力"，就不给它"信息本身"。** 给信息是一次性的，必过期；给能力是持续有效的——它自己去取，永远是最新的。Cursor 做过 A/B 测试：把 MCP 工具描述改成按需加载、只给工具名、用的时候再查，调用过工具的 runs 里 token 消耗下降 46.9%——这就是"给指针不给内容"的工程实证。

**阶段四：动手修复（第二问：去坏清单）**

Agent 准备改代码时，用第二问：**哪些信息它看一眼就会信、但绝不能信？** 这阶段最危险的不是"缺信息"，是"信了错信息"：

- 代码注释可能过期——注释说"这里永不执行"，但逻辑已经变了。让 Agent 以代码实际行为为准，注释只当线索不当证据。
- 旧文档可能误导——`docs/v2` 的接口说明，线上可能已经换成 v3。维护一份"不要信什么"清单（"`docs/v2` 已废弃，以 `docs/v3` 为准"、"这个接口已下线"），Agent 就不会被旧资料带偏。
- 涉及金钱、权限、合规的判断——要求 Agent 至少两个来源一致才采信，且必须引用来源。

**多数 Agent 失败不是"没信息"，是"信了错信息"——去坏清单往往是最快见效的一个动作。**

**阶段五：验证（贯穿全程的判断主线）**

验证不是第五步，而是**每一步都在做**的判断。从"复现出 bug"（验证症状真实存在）到"改了代码跑测试"（验证修复有效）到"跑全量回归"（验证没弄坏别的），Agent 每走一步都要能回答：**我怎么知道自己这一步做对了？**

两个落地动作：一是给运行验证的手段——让它能跑测试看报错、能查状态看结果；二是把验收标准写进任务（阶段一已经做了）——Agent 每改一步就对照验收标准自检，而不是改完才想起来"哦我没跑测试"。反模式是只给文档不给验证手段：知识库路线最典型的死法，就是它能检索到"最相关的片段"，却没有任何方式确认自己理解对不对。

**阶段六：收尾（第三问：窗口管不住时）**

修完 bug、测试全绿，任务还没完。长任务里上下文一定膨胀，用四招收尾：

- **写窗外**：把这次排查的结论写进 NOTES.md 或 CLAUDE.md——下次遇到同类 bug 就不用重新查一遍。写在文件里，别留在对话里（对话会被清掉）。
- **该记才记**：跨会话要用的稳定事实（"这个模块的测试命令是 X""这个配置项有 Y 的限制"）写进 CLAUDE.md。但要克制——MEMORY.md 每次会话只加载前 200 行或 25KB，超过就截断；别把记忆堆成第二个知识库。
- **能清就清**：换下一个任务前，清掉这个任务的历史——新任务开新会话，别让上个任务的记忆污染下个任务。
- **一个任务一个窗口**：如果任务大到要拆（比如同时改三个模块），把深挖的活丢给子代理——它带干净上下文去查，只回传浓缩摘要，主 Agent 的窗口保持干净。这就是隔离（isolate）。

这四招就是 Anthropic 官方语境管理的 write / compress / clear / isolate——翻译成人话：写窗外、能清就清、该记才记、一个任务一个窗口。

### 进阶：当症状和根源分离——缺陷挖根因

上面六阶段覆盖了"定位到问题就能修"的通用情况。但真实大仓里最难、也最考验上下文工程的，是**症状和根源分离**的情况——**crash 在这里，根源在别处。** 我们用六阶段里"确认根因"这一阶段（阶段三）做深化：当你发现"表层崩溃点不是引入点"时，往下怎么挖？用一个真实的多层因果链走一遍：

> **表层 crash 在 A**：A 断言 `state == READY` 失败，state 是 STALE——这是保护性措施，A 故意在状态不对时崩溃，防止基于错误状态往下处理。
> **往回推一层**：状态为什么是 STALE？本该有个线程 T 把它推进到 READY，但 T 没做。
> **再往回推**：T 为什么没做？T 被阻塞在锁上——另一个进程 B 死锁了（B 和 C 以相反顺序加锁），T 一直拿不到锁。
> **根因**：B 的死锁（并发问题），不是 A 的状态断言。

这个进阶场景，是阶段二"剪枝"和阶段三"确认根因"在深挖情形下的深化应用——下面会看到，通用的"修剪工具返回"在这里升级为"深挖多层的持续剪枝"，通用的"来源路由"在这里升级为"补设计意图"。

#### 核心：上下文工程的关键手段

**先想清楚：深挖到底需要什么？** 这个场景最难的一步，是判断 **A 的断言是"防线"还是"冗余"**——同样的 `assert state == READY`，两种设计意图都成立：

- 如果状态机契约规定"READY 是 A 的处理前提、上游本应保证"，那 A 的断言是**防线**——去掉它就是把保护性 crash 关掉，脏数据继续流向下游，造成更严重、更隐蔽的损坏；
- 如果契约允许 STALE 时 A 也要处理，那 A 的断言是**冗余**——修 A 才对。

**而这个判断，从 A 的代码里读不出来。** 代码只呈现"事实"（这里有个断言），呈现不了"设计意图"（这个断言为什么存在、该不该存在）。判断防线还是冗余，需要的是**设计意图**，不是代码事实——这就是挖根因要补充给模型的有效上下文。**它不是更多代码（模型读代码自己就能拿到事实），而是代码背后的设计意图。**

所以"深挖"的上下文工程，本质是两个手段：**一是让模型能取到设计意图（补），二是让深挖的每一层都能持续进行（剪）。** 两者缺一不可——光补不剪，窗口很快爆掉；光剪不补，模型没有判断依据。

**手段一：补设计意图——让模型能判断"该往哪挖"。**

具体到这个场景，判断防线还是冗余、以及往哪挖，需要四类设计意图，**没有一类能从崩溃点 A 的代码事实里读出来**：

| 设计意图 | 这个场景里是什么 | 从哪取（运行时，不是预置） |
|---|---|---|
| **接口契约** | state 在契约里是不是 A 的处理前提？A 的断言是不是防线 | 状态机定义、下游消费逻辑 |
| **变更动机** | 这个断言是哪个提交、为什么加的 | git blame → 定位提交 → 读 commit message / issue / PR |
| **设计决策** | 状态本应由谁、何时更新到 READY | 状态机/责任模型、设计文档 |
| **领域规则** | 这个状态在业务上可能停留多久、什么会推进它 | 领域知识、规范文档 |

契约、动机、决策、规则，全在代码之外——在 git 历史、issue、ADR、领域知识里。**所以手段一是给模型建好"按需取设计意图"的通道**：让它每挖一层，能取到这一层的契约/动机/决策/规则，而不是取到一堆代码事实。这个通道包括：契约的指针（状态机定义在哪）、git 定位变更动机的能力、跨文件对比（找加锁点、找状态赋值点）、运行工具（看 T 是否真的卡住、B 是否真的死锁）。注意，设计意图靠**运行时去取**（git blame 的动机、进程的实际状态），不是预置进知识库——这又一次说明"知识库存代码事实是冗余、存不进设计意图"。

**手段二：上下文剪枝——让深挖的每一层都能持续。**

阶段二讲过通用的"修剪工具返回"——那是管单次工具返回的进。深挖时这个手段要升级：**每一层都要剪，而且是"取完这层就剪掉，让位给下一层"**。这里有个必须点破的关系：**"取设计意图"和"剪枝"不是两件矛盾的事，而是同一循环的两半。** 深挖要追多层（A→状态→T→B），每层的设计意图都是**临时**的——判断完这一层、确认要不要继续往上游，这一层的内容（大段源码、日志、中间推理）就该让位给下一层。如果不剪，挖到第 3 层时第 1、2 层的输出还堆在窗口里，还没到根因窗口就满了。

**剪枝是上下文工程的关键手段**：管理"窗口里始终只装此刻要用的"，让深挖能持续进行下去。三件事，配合"外部化"才无损：

- **剪表层**：PostToolUse hook 用 `updatedToolOutput` 把崩溃点 A 的大输出截断（超 2000 字符只留摘要）——没进来过的信息没占过预算，模型也拿不到"修表层的捷径"；
- **先外部化，再剪枝**：PostToolUse hook 把每层工具调用写到 trace 文件（外部化），**写出去之后，窗口里那份就可以清掉**——外部化是剪枝的前提，先落盘再清，才不丢信息；
- **剪中间层**：挖到第 N 层时，前面 N-1 层的中间结论只保留"这一层取到的设计意图摘要"，大段源码、日志、中间推理清掉——让窗口始终只装着"当前层 + 已确认的设计意图链"。

所以完整循环是：**每层 → 取这一层的设计意图 → 外部化到 trace → 从窗口剪掉 → 只留摘要进下一层**。取和剪交替进行：**取是"进"，剪是"出"，一进一出，窗口保持精简，深挖才能逐层走到底。**

#### 配套：保障落地手段——保证模型真的"取 + 剪"

但这里有个残酷的现实：**上面两个手段给了模型"能取能剪"的能力，不等于模型会用。** 模型被训练成"想尽快完成任务"，它天然倾向停在表层——修 A 的断言让测试变绿就宣布完成，根本不会去走"取设计意图 → 剪枝 → 再挖一层"这条循环。它会**绕过**通道，直接停在表层答案上。

所以需要**保障机制**，让模型真的去取、取够才许停。保障不是主角，是配套——**它不替代"补设计意图 + 剪枝"这两个手段，只保证模型真的执行它们。** 两个保障：

**保障一：Stop hook——不取够设计意图不许停。**
模型每次想停（宣布完成）时，被 Stop hook 拦截检查——是否真的挖到了根因、是否取到了契约/动机/决策/规则。不通过就返回 `decision: "block"`，把"还缺哪层设计意图、继续挖"作为新指令喂回去。这是官方明文的确定性机制（"hooks are deterministic and guarantee the action happens"），不是提示词。注意：**真正强制不结束的是 `decision:"block"`，不是 `continue:false`（那是硬停止）。**

`.claude/settings.json`：

```json
{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "agent",
            "prompt": "验证这个 bug 是否真的挖到了根因：
1. 是否只动了崩溃点 A 的表层（去掉断言）？如果是，说明还没取到上游的设计意图。
2. A 的断言是防线还是冗余，是否查了状态机契约（接口契约）？
3. 状态为什么 STALE、线程 T 为什么没推进它、B 是否死锁——是否都取到了对应层的设计意图（责任归属、变更动机）？
4. 是否用测试锁定了根因？
如果不满足，返回 {"ok": false, "reason": "还缺哪层设计意图，继续挖"}。",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
```

这个 `type:"agent"` 的 Stop hook spawn 一个**带工具的独立验证子代理**（能读文件、跑测试，最多 50 轮），检查证据、返回 `ok:false` 时 reason 喂回主模型强制继续。**模型想"修完表层就走"时，它拦下，逼它去取上游设计意图。**

**保障二：验证作为阶段门槛——每层取到设计意图才允许进下一层。**
- **SubagentStop hook**：让每一层用一个独立子代理，它想结束时被检查——如果这层的设计意图（如"确认了 T 为什么没更新状态"的责任归属）没取到，`decision:"block"` 强制它继续；
- **Dynamic Workflows（官方确定性编排）**：用 JavaScript 脚本持有控制流，逐层 spawn 子代理、每层用 `schema` 强制结构化输出、层间用纯 JS 判定。官方原文："The plan is enforced by code... Nothing depends on Claude remembering an instruction."——**代码决定什么时候该取下一层设计意图、什么时候该停**，不靠模型自觉。

#### 诚实交代：保障能硬到什么程度

这套配套**能硬保证的是"不取够设计意图不许停"**，**不能硬保证"挖得对"**：

- **能硬的**：模型想停在表层 → Stop hook 确定性拦截 → 强制继续；每层验证不过 → 编排层不让进下一层。这是 2026 年 Claude Code 原生支持、官方明文的确定性机制。
- **不能硬的**：hook 只能检查**证据痕迹**（测试结果、标记文件、回复文本），**检查不了模型的认知状态**——模型可以"假完成"。而且有 **8 连击上限**：连续 8 次想停都被拦、且无进展时，系统强制放行。
- **所以准确的说法是**：上下文工程提供"该补什么、怎么管窗口"（补设计意图 + 剪枝），保障机制提供"不许不补就停、每层取到才进下一层"（Stop hook + 验证门槛）。前者是本，后者是配套——**保障不能替代上下文工程本身，只保证它被做到。**

#### 怎么判断要不要上这套？先裸跑

让 Agent 裸跑一次那个 bug：如果它"停在表层、修表面症状就完成"，说明"想尽快完成"偏置正在起作用——需要配套保障，让它真正去取上游的设计意图；如果它自己能一路挖到引入点（B 的死锁），说明你的任务还不需要这么重。和前面的原则一致：机制是跑出来的需求，不是预先铺满。

### 怎么开始：先跑，再判断

不要先建库、先清洗、先索引。**先让 Agent 裸跑一遍任务**——只给任务描述，不给任何项目上下文。裸跑的意义不是"看它卡不卡"，而是**用你已经梳理好的领域标准，对照暴露"缺什么"**。卡住的位置就是你的工作清单，一次补一处：

| 它卡在哪 | 缺什么 | 补什么 |
|---|---|---|
| 原始缺陷看不懂、无法复现、不知期望 | 缺标准 + 缺预处理 | 先梳理该领域的必要上下文（标准），再上预处理 Agent 按 schema 结构化（阶段零） |
| 找不到入口、不知道命令 | 缺指针 | 给一行路径、一个命令，不是整篇文档 |
| 猜错约定、按旧规则执行 | 缺规程 | 写一页"Agent 手册"（测试命令、代码位置、易错约定，压到 200 行内） |
| 编造 API、重复犯错 | 缺验证 | 给它跑测试看报错的能力；把验收标准写进任务 |
| 上下文不足却给确定答案 | 缺置信度分层 | 让它学会按上下文强度给结论——信息不足就说"确定不了，需要 X" |

裸跑就完成？什么都别做，你的任务不需要上下文工程。在软件工程场景里，这个判断很干脆：一个需要读写代码、跑测试、改配置的任务，几乎没有"裸跑就能稳定完成"的可能——所以上面这些几乎总是会用到。

### 收尾：三问 vs 知识库，动态 vs 静态

知识库其实也回答这三问，但只在建库时回答一次：检索什么、索引从哪来、数据质量怎么过滤。上下文工程的区别不是问了不同的问题，而是**每个关键决策点都在重新回答**——因为动作在变、验证标准在变、信任边界在变。一句话钉死：

> **知识库是"一次性回答三问"，上下文工程是"每步重新回答三问"。前者把世界冻结在建库那一刻，后者活在运行的每一刻。**

下一节的对照实验会证明这个差别：同一个任务，路线 A 走知识库、路线 B 按上面这套搭环境，结果截然不同。

## 实测：同一个模型，两条路线，结果截然不同

前面全是论证，这一节给你看一个真实的对比实验——不是论文里的数字，是我们刚刚亲手跑的。

我们取了一个真实开源项目（loguru，一个 Python 日志库），在它的副本上做了三处企业里常见的"私有化改造"：把内部函数重命名、加了一个上游没有的配置文件、改了一个 token 的精度实现。改造完成后全量测试仍然 1598 个全部通过——也就是说，这不是一个"故意弄坏"的仓库，它就是一个正常的、像你们公司那样的私有 fork。

然后我们给同一个模型（同一个 agent）同一个任务：**在这个 fork 上实现一个新功能**（加一个新的时间格式 token，并遵守那个私有配置文件的精度设置）。唯一不同的是两条路线的"上下文配置"：

**路线 A：知识库路线。** 把仓库的 README、文档、源码切成了 1091 片，做了向量索引。模型只能通过检索到的片段来了解这个仓库、完成功能。结果：检索确实命中了一些相关片段（比如找到了 token 注册函数的开头），但切片把关键实现截断了，而且模型**没有运行验证的手段**——它最终交付的是一份"无法验证的猜测"。它甚至不知道一件决定成败的小事：那个格式化正则里根本没有新的 token 名，不更新正则，功能永远不会被触发。

**路线 B：上下文工程路线。** 给模型一份 200 行的操作规程（怎么跑测试、代码结构在哪、有个配置文件要注意什么），加上工具（读文件、搜索、跑测试）。模型自己 grep 定位、读完整实现、发现正则缺 token、实现功能、写测试——第一次跑挂了 5 个测试，它逐个修复（一个闭包作用域 bug，一个测试断言问题），最后全量 **1603 个测试通过**（1598 原有 + 5 个新增），零回归。

对比结果：

| | 路线 A（知识库） | 路线 B（上下文工程） |
|---|---|---|
| 完成功能 | ❌ 交付无法验证的猜测 | ✅ 完成 |
| 全量测试 | 无法运行 | 1603 通过，0 回归 |
| 定位关键信息 | 部分命中但被切片截断 | 完整读取 |
| 发现"正则缺 token" | 不知道 | ✅ 发现并处理 |
| 迭代修复 | 无此能力 | 2 轮修复 |
| 运行验证 | 结构性缺失 | 核心闭环 |

这个实验说明的不是"知识库在软件工程里一无是处"——静态问答场景不在本文讨论范围。它说明的是：**一旦任务从"回答一个问题"变成"完成一件持续的工作"（软件工程里的任务几乎都是后者），知识库路线的结构性缺陷就暴露了**：它能给你"最相关的片段"，给不了"验证自己有没有做对的能力"。而后者，恰恰是 Agent 能不能稳定、正确、持续工作的全部关键。

实验是公平的：同一个仓库、同一个任务、同一个成功标准、同一个模型，连知识库路线都允许换词重试（模拟真实用户）。完整过程（三处改造、1091 个切片、检索结果、修复日志）都在仓库的 `experiments/` 目录下，含 `experiments/README.md` 的复现步骤和全部脚本——你可以自己跑一遍验证。
